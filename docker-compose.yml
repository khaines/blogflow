services:
  blogflow:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./examples/content:/data/content:ro
      - ./examples/config:/data/config:ro
    environment:
      - BLOGFLOW_SERVER_PORT=8080
