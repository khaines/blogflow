apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "blogflow.fullname" . }}
  labels:
    {{- include "blogflow.labels" . | nindent 4 }}
data:
  site.yaml: |
    {{- .Values.siteConfig | toYaml | nindent 4 }}
